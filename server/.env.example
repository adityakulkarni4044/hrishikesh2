PORT=5000
MONGO_URI=mongodb://127.0.0.1:27017/nowwagon
JWT_SECRET=change_this_to_a_long_random_secret

# Optional: if provided, server will email reset links.
SMTP_HOST=
SMTP_PORT=
SMTP_USER=
SMTP_PASS=
SMTP_FROM="Nowwagon <no-reply@nowwagon.local>"

# Frontend base URL used to build reset links
CLIENT_URL=http://localhost:5173

